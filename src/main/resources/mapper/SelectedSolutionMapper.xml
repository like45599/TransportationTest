<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sdjt.mapper.SelectedSolutionMapper">

    <insert id="insertSolution" parameterType="com.sdjt.pojo.Plan">
        INSERT INTO selected_solutions (question, solution, reference, project_id)
        VALUES (#{question}, #{solution}, #{reference}, #{project_id})
    </insert>

    <!-- 这里可以添加更多的SQL操作 -->

</mapper>